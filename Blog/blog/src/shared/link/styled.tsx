import { styled } from "@mui/material";
import { Link } from "react-router-dom";

export const AppLink = styled(Link)(() => ({
  fontSize: "14px",
  fontWeight: "bold",
  color: "#00bcff",
  marginTop: "5px",
  textDecoration: "none",
}));
